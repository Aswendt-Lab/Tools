# CreateGroupMapping

`Create_GroupMapping.py` opens a Tkinter GUI, recursively finds subfolders whose names contain a supplied case-sensitive string, and writes the matching folder names to a text file selected by the user.

```bash
python Create_GroupMapping.py
```

Only folder names are written, not full paths, so duplicate names from different locations can appear in the output.
